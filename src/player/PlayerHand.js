import React from 'react';
import CenteredContainer from '../containers/CenteredContainer';

export default ({game}) => {

    if(game){
        console.log(game);
    }
    return(
        <CenteredContainer>
        
        </CenteredContainer>
    );
}